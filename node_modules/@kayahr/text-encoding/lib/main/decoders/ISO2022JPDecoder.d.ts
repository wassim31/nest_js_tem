import { AbstractDecoder } from "../AbstractDecoder.js";
import { ByteBuffer } from "../ByteBuffer.js";
/**
 * Decoder for iso-2022-jp encoding.
 */
export declare class ISO2022JPDecoder extends AbstractDecoder {
    private state;
    private lead;
    private outputFlag;
    /** @inheritDoc */
    decode(buffer: ByteBuffer): null | number | number[];
}

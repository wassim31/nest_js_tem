import { AbstractDecoder } from "../AbstractDecoder.js";
import { ByteBuffer } from "../ByteBuffer.js";
/**
 * Decoder for gb18030 and gbk encoding.
 */
export declare class GBDecoder extends AbstractDecoder {
    private first;
    private second;
    private third;
    /** @inheritDoc */
    decode(buffer: ByteBuffer): number | number[] | null;
}
